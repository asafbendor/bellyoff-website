name = "bellyoff-blog-cron"
main = "blog-cron.js"
compatibility_date = "2026-06-14"

# Fires daily at 07:00 UTC; the worker itself publishes only once every 3 days
# (guard in scheduled(), PUBLISH_EVERY_DAYS in blog-cron.js).
[triggers]
crons = ["0 7 * * *"]